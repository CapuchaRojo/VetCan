// api/tests/callbacks.ai.test.ts

/**
 * Phase 4 guardrail tests.
 * These intentionally start as placeholders.
 * Codex should expand behavior but must preserve non-PHI constraints.
 */
describe("AI Callback (manual)", () => {
  it("rejects medical questions", async () => {
    expect(true).toBe(true);
  });

  it("flags staff follow-up when needed", () => {
    expect(true).toBe(true);
  });
});
